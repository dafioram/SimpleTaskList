# ==========================================
# PROJECT IDENTITY
# ==========================================
APP_NAME="SimpleTaskList"
DESCRIPTION="Track Tasks and Their Status"
PYTHON_BIN="python3"

# ==========================================
# DIRECTORY STRUCTURE
# ==========================================
# Use "app" or "src" if your code is nested.
# Use "." if your main.py and requirements.txt are in the root.
APP_DIR="."

# ==========================================
# RUNNER CONFIGURATION (Select One)
# ==========================================

# --- OPTION 1: Standard Python Script ---
ENTRYPOINT="python"
ARGS="${INSTALL_DIR}/${APP_DIR}/main.py"

# --- OPTION 2: Uvicorn (FastAPI) ---
# ENTRYPOINT="uvicorn"
# ARGS="main:app --app-dir ${INSTALL_DIR}/${APP_DIR} --host 0.0.0.0 --port $PORT"

# --- OPTION 3: Gunicorn (WSGI) ---
# ENTRYPOINT="gunicorn"
# ARGS="wsgi:app --chdir ${INSTALL_DIR}/${APP_DIR} --bind 0.0.0.0:$PORT"

# ==========================================
# ENV VARS (Static)
# ==========================================
export PYTHONUNBUFFERED=1